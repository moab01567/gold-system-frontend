services:
  web:
    image: node:22.12.0
    working_dir: /app
    volumes:
      - ./:/app
      - /app/node_modules
    command: sh -c "npm install && npm run build"
    env_file:
      - ./.env
    user: "${UID}:${GID}"
